export interface Course {
    title: string,
    level: string,
    language: string,
    instructor: string,
    duration: number,
    rating: number,
    enrollmentCount: number,
    isFree: boolean,
}
